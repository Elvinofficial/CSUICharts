<UserControl 
    x:Class="UICharts.Desktop.Views.ToolboxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:vm="clr-namespace:UICharts.Desktop.ViewModels"
    xmlns:local="clr-namespace:UICharts.Desktop.Views"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">

    
    
    <Border 
        Grid.Row="0"
        Grid.RowSpan="3"
        Grid.Column="0"
        CornerRadius="25"
        BorderBrush="Black"
        BorderThickness="2"
        Background="#ECECEC"
        Padding="10"
        Margin="-19,-15,5,-15"
        Grid.ColumnSpan="2">
        
        <Grid>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="57*"/>
                <RowDefinition Height="91*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

           <TextBlock Text="Фигуры" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>

           <ListBox
                ItemsSource="{Binding Figures}"
                SelectedItem="{Binding SelectedFigure}"
                DisplayMemberPath="Name"
                Background="#ECECEC"
                Margin="0,43,0,39"
                FontSize="16" Grid.RowSpan="4"
                BorderBrush="#ECECEC"
                HorizontalContentAlignment="Center"
                RenderTransformOrigin="0.508,0.536"/>

           <TextBlock Grid.Row="3" Text="{Binding SelectedFigure.Name, StringFormat=Выбрано: {0}}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"/>
        
        </Grid>
    </Border>
</UserControl>
